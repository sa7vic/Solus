import DocGenerator from "./DocGenerator.jsx";

export default function CanvasComparison() {
  return (
    <DocGenerator
      format="docx"
      placeholder="e.g. Compare three compressor vendor quotes (Atlas, Kirloskar, Ingersoll Rand) on price, delivery time, and warranty terms, and flag anything missing from standard terms."
    />
  );
}
